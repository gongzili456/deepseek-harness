pnpm install 后自动安装 lefthook 钩子；统一入口包括 `pnpm build`（host+client+web）、`pnpm test`（含 100% 覆盖率门控）、`pnpm check:all`（CI 全量门禁）、`pnpm hygiene`（vendor 重 scope/knip/publint/constraints/license/invariant 全量校验）、`pnpm docs:build`（VitePress 构建并校验片段）、`pnpm release:*`（bump/verify/pack/publish 流水线）。