本地文档变更需运行 `pnpm run doc-sync` 触发 `verify-translation-pairing`；编辑任意一侧后须用 `pnpm run verify-translation-pairing --write <pair>` 重新记录 blob hash 以确认一致性；排查合并冲突可使用 `pnpm run resolve-translation-pairing-conflicts`。