sandbox-local 在首次 confine() 前会按 PLATFORM_CHAINS 顺序对 bwrap/landlock/seatbelt/windows-acl 执行一次功能探测（spawnSync 带 probeTimeoutMs），失败则抛出 SandboxUnavailableError；Windows ACL 路径需要安装 node-addon-landlock-run 提供的 landlock launcher 并在生产环境存在 lib/runner.js，否则回退到 tsx 加载源码入口。