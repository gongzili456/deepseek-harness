schedule 包受 AGENTS.md 约束：运行时只能订阅未来发布的根 Agent，不得扫描已持久化 Session；创建/删除操作需先 await `ctx.sessions.flush(session)` 再等待第二次 barrier 确认持久性。preset/agent-presets 通过文件系统 `mtimeMs`+`size` 戳管理 standing mount 代际，编辑组合文件会触发新代际而非销毁旧实例。