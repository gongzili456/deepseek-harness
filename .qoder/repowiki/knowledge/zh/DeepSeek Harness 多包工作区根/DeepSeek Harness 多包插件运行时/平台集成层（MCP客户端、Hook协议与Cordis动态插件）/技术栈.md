TypeScript ESM；MCP 客户端使用 `@modelcontextprotocol/sdk` 与 `zod` 进行 schema 校验；Cordis 动态插件通过 `@deepseek-ai/cordis` Fiber + `@deepseek-ai/dsh-typert-protocol` 的 `@Remote` 装饰器实现 Host/Client 双向 RPC；UI 侧使用 React 18 与 CSS Modules，构建脚本为 `tsdown`。