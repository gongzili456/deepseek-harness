`cordis-client-runner` 与 `ui-cordis` 通过 `package.json` 的 `dsh.client.inject` 数组声明需注入的 client 运行时依赖，并使用 `npm run bundle`/`watch`（基于 tsdown）生成 `lib/client.js`；`mcp-client` 支持两种传输：stdio 模式通过 `command`+`args` 启动子进程，streamable-http 模式通过 `url`+`headers` 直连 MCP server。