Web 搜索 provider 需通过环境变量或配置注入 API Key（如 `$DEEPSEEK_API_KEY`、`$DSH_WEB_SEARCH_PROVIDER`、`$DSH_WEB_FETCH_PROVIDER`）；HTTP fetch provider 默认禁止跟随重定向（`redirect: 'error'`），需在部署时显式开启；附件根目录由 `DSH_HOME/attachments/v1` 管理，溢出文件默认写入进程私有 `tmpdir/dsh-spill-*` 目录。