`subprocess-local/scripts/ensure-spawn-helper.mjs` 在构建/启动阶段确保 spawn helper 可用；各包通过各自 `package.json` 的 `inject = ['subprocess'|'sandboxPolicy']` 声明 cordis 依赖，部署时由顶层 `cordis.yml` 选择具体 executor/provider 组合。