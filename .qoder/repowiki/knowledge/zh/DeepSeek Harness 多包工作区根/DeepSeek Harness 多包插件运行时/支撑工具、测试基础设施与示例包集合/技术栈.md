TypeScript + Cordis 插件框架（Context/plugin/effect/fiber）；配置校验统一使用 `@deepseek-ai/schemastery`；invariants 子系统通过 `@deepseek-ai/cordis` 的 Service 机制挂载；测试套件基于 Vitest；CLI 入口使用 tsdown 打包二进制（如 llm-mock-server/bin.ts）。