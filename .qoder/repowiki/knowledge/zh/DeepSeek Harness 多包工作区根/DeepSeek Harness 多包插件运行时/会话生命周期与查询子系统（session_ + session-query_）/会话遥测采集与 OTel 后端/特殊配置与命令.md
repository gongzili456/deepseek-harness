OTel 后端在构造阶段即校验 `exporter.url`（必须为 http(s)）、`processor.maxExportBatchSize`（正整数）和 `shutdownTimeoutMillis`（有限正值且不超过 Node 定时器上限 2147483647），非法配置直接抛错阻止插件加载；`DISABLED` 模式下不初始化任何 OTel 资源，仅对 `feedback/record` 事件输出本地警告。