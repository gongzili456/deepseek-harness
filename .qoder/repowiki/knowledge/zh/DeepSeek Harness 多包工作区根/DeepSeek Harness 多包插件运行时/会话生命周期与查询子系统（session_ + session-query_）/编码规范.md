- 每个子包统一暴露 `src/index.ts` 作为对外 API，并在同级 `src/invariant.ts` 中集中放置运行时不变量校验。
- 通过 Cordis 上下文键（如 `ctx.sessionPersistence`、`ctx.sessionProjections`、`ctx.sessionTitle`、`ctx.sessionQuery`、`ctx.tools`）进行服务装配，而非直接依赖。
- 每个子包附带 `README.md`、`README.zh.md`、`README.i18n.yaml` 三语言文档，保持多语言说明同步。
- 测试目录使用 `*.spec.ts` 命名单元测试、`*.e2e.ts` 命名端到端测试，并按功能拆分 fixture。