- 投影单元统一实现 `ProjectionDefinition` 接口，通过 init/apply/view/stateVersion 四件套声明纯函数式状态机，apply 对不感兴趣的事件必须返回同一 state 引用以便 Object.is 短路变更通知。
- 所有 view 输出先经对应 Zod schema.parse 再进入变更通知或快照，确保跨进程/持久化的值契约一致。
- 通过 `declare module '@deepseek-ai/cordis'` 扩展 Context 接口（如 `ctx.sessionProjections`、`ctx.sessionProjectionCache`）来注入服务，而非直接 import 构造。
- 注册与订阅均以 Cordis effect 形式返回 disposer，随 fiber 生命周期自动清理（如 register/onChanged 的 dispose 回调）。
- 持久化写路径一律 fail-soft：flushSoft/putSoft 捕获异常仅记 warn 日志，保证缓存丢失只会导致下次冷读重放更长尾部，不会破坏主流程。
- 检查点行携带 stateVersion，恢复时 ver 不匹配即丢弃旧行并从 init 重折叠，避免跨版本状态迁移。