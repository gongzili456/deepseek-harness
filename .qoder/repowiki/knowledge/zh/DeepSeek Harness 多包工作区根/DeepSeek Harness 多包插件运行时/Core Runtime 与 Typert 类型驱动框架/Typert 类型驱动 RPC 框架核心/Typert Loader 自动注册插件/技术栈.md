TypeScript ESM 插件，基于 @deepseek-ai/cordis 插件系统；使用 @deepseek-ai/schemastery 定义 Config schema；通过 Node `node:module.createRequire` + `pathToFileURL` 动态导入宿主面 artifact；manifest 校验依赖 zod v4 schema 实例（`_zod` 标记）。