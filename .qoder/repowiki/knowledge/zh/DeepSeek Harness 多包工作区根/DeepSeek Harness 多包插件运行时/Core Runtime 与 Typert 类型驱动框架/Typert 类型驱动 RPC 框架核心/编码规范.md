- 每个子包独立维护 package.json、tsconfig.json 与 README.i18n.yaml，对外只暴露 src/index.ts 作为入口。
- 所有包统一通过 invariant.ts 提供运行时不变量检查，并在测试中覆盖异常路径。
- 跨包通信严格通过 protocol 定义的 RPC 类型与装饰器进行，禁止直接引用对方内部实现。
- 运行时扩展点统一通过 Cordis 上下文键（ctx.typert、ctx.loader）注入与消费。