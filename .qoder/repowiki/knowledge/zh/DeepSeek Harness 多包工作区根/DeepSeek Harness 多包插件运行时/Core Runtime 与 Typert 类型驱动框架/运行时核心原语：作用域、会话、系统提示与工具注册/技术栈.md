基于 `@deepseek-ai/cordis` 的 Context/Service/Events/Fiber 作用域与插件机制；使用 `@deepseek-ai/schemastery` 进行配置与 schema 校验；通过 `@deepseek-ai/dsh-llm` 的 `deepFreeze` 与 `Message` 类型保证不可变性与 LLM 消息契约；TypeScript 单包独立 tsconfig/tsdown 构建。