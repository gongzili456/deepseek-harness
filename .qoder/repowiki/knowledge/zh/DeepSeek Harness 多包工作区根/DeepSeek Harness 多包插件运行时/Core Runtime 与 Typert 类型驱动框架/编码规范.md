- 每个子包通过 `declare module '@deepseek-ai/cordis'` 扩展 `Context` 接口（如 `ctx.sessions`、`ctx.tools`、`ctx.agentLoop`）和 `Events` 命名空间来定义服务间通信契约。
- 所有公共 API 入口文件附带 JSDoc 模块注释，并在同目录维护 `README.md` / `README.zh.md` / `README.i18n.yaml` 三语文档。
- 配置与参数校验统一使用 `@deepseek-ai/schemastery` 的 `z.object({...})` 声明，并在 Service 的 `static Config` 或构造函数中消费。
- 每个子包提供 `src/invariant.ts` 集中存放运行时不变量断言，测试侧配套 `tests/invariant.spec.ts` 验证。
- 跨包依赖通过 npm 包名（`@deepseek-ai/dsh-*`）引用，而非相对路径，保持 core 与 typert 两套包族之间的解耦。