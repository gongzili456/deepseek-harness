`deepseek-harness-runtime-bin` 为 wheel-only 包，禁止 sdist 构建（`hatch_build.py` 中显式 raise）；需通过 `scripts/build-exe-for-python-sdk.ts` 生成平台二进制后由 CI 发布；本地开发可通过设置 `DSH_RUNTIME_MODE=node` 或 `uv` editable 源指向 `../sdk-runtime` 切换至源码 Node 模式运行。