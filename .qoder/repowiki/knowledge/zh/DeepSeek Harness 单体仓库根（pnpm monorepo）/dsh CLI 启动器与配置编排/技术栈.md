TypeScript + ESM；CLI 参数解析使用 commander@15；打包使用 tsdown（entry 指向 `lib/types/bin.js`）；运行时依赖 @deepseek-ai/cordis 生态（cordis-plugin-loader/include/timer/hmr）与自研 dsh-app-boot/dsh-cmdline/dsh-launch-environment 等 workspace 包。