构建产物位于 `lib/`，由 `tsdown` 仅打包 `lib/types/bin.js`；发布时 `files` 字段包含 `lib/*.js` 与 `config/` 目录，因此 `config/agent-presets/` 必须随包分发。运行 `pnpm dsh --profile <name>` 启动指定 profile，`dsh plugin --profile <name> <pnpm args>` 管理 profile 插件（需系统 PATH 中存在 pnpm）。