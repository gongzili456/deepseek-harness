各包通过 `tsdown.config.ts` 构建，脚本统一为 `npm run bundle` / `npm run watch`；`gateway` 与 `remotes` 使用分离的 `tsconfig.client.json` 与 `tsconfig.host.json` 编译双端产物；`cordis-client-runner` 与 `ui-cordis` 通过 `package.json.dsh.client.inject` 声明浏览器注入依赖，需由宿主 Loader 发现。