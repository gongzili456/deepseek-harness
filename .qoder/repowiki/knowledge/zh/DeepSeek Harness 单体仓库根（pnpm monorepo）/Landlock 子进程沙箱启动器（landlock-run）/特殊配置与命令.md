开发需 `corepack enable && pnpm install`，然后 `pnpm build:ts` 编译 JS、`pnpm build:native` 在安装了 `musl-tools` 的 Linux 主机上本地编译二进制；非 Linux 平台会直接退出。发布流程通过 `release:bump` → `release:assemble-prebuilds` → `release:pack` → `release:publish` 系列脚本完成。