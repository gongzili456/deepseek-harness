构建产物输出至 `lib/`，通过 `tsdown.config.ts`（app-boot）或默认 tsconfig 编译；测试位于各自 `tests/` 目录并使用 Mocha 风格 spec 文件（如 `app-boot.spec.ts`、`config-dump.spec.ts`、`config-reload.spec.ts`、`hmr-config.spec.ts`、`profile.spec.ts`、`user-patches.spec.ts`、`cmdline.spec.ts`），需在工作区中安装 workspace peer 依赖后方可运行。