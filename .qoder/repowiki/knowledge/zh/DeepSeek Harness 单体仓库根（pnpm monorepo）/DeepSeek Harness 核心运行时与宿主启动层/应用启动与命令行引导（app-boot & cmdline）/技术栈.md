Node.js ESM + TypeScript；YAML 解析使用 `js-yaml` 配合 `cordis-plugin-include` 的 `entryListSchema`/`userPatchesSchema` 实现 `!!js` 表达式插值；CLI 参数解析使用 `commander`；进程级失败保护通过监听 `unhandledRejection` 并在可释放超时后调用 `process.exit(1)`。