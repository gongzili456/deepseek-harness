通过 `package.json` 的 `exports` 字段暴露多入口（`.`、`./surface`、`./invariant`、`./types`），构建产物位于 `lib/`，类型声明位于 `lib/types/`；需 workspace 中已存在 peer 依赖 `@deepseek-ai/dsh-brand`、`@deepseek-ai/dsh-invariants`、`@deepseek-ai/dsh-llm`、`@deepseek-ai/dsh-scope`、`@deepseek-ai/dsh-typert-protocol`、`@deepseek-ai/cordis`。