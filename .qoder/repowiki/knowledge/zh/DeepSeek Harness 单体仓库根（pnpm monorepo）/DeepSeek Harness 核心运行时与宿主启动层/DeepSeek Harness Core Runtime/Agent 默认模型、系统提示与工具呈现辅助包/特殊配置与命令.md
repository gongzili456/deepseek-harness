每个子包均为独立 npm 包，拥有各自的 `package.json`、`tsconfig.json`、`README.i18n.yaml` 多语言文档与 `tests/` 目录；`agent-tool-presentation` 作为 Cordis Plugin 需通过 `apply(ctx, config)` 在 preset 作用域内挂载，且 code 模式要求部署中已组合 `codeRuntime` 服务。