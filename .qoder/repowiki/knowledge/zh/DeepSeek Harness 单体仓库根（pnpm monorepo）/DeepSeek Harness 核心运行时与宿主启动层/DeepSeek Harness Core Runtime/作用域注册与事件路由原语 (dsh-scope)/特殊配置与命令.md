运行 `pnpm run gen-scoped-events` 重新生成 `src/scoped-events.generated.ts`（事件名到参数索引的解析映射）；构建产物由 `tsdown.config.ts` 定义，分别输出根入口与 `invariant` 子入口，并在 invariant 构建中通过 `deps.neverBundle: ['@deepseek-ai/dsh-scope']` 避免重复打包以保证 WeakMap 单例。