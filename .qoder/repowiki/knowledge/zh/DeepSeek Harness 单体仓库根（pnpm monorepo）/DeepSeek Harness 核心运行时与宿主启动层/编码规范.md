- 每个子包同时导出主入口与独立的 `./invariant` 字段，供运行时断言与测试使用
- 对外 API 通过 zod schema 定义并在 apiproxy 中强制校验，保证跨进程通信契约稳定
- 子包之间以 workspace:^ 声明 peerDependencies/devDependencies，避免重复安装 Cordis 实例
- 所有运行时包遵循统一的 tsdown 构建产物布局（lib/index.js + lib/types/*.d.ts + lib/invariant.js）