包名为 `@deepseek-ai/dsh-subagent`，通过 `package.json` 的 `exports` 同时暴露主入口、`./invariant`、`./client` 以及 `./src/*` 源码路径；构建产物位于 `lib/`，仅发布 `lib/index.js`、`lib/invariant.js` 及 `lib/types/**/*.js|d.ts`。测试位于 `tests/*.spec.ts`，需安装 workspace 内全部 peer dependencies（agent、session、sandbox、jobs、user-approval 等）方可运行。