TypeScript ES Module；基于 `@deepseek-ai/cordis` 的 Service/Context 容器模型；使用 `zod` 做 JSON Schema 断言；通过 `@deepseek-ai/dsh-scope` 的作用域目标实现事件按父 Agent 过滤；依赖 `@deepseek-ai/dsh-session`/`dsh-session-persistence`/`dsh-session-projection` 完成会话枚举与投影。