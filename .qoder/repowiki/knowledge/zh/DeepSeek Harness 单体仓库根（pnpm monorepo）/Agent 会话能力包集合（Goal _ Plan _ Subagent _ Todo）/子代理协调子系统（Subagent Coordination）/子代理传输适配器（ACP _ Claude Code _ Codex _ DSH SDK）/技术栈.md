TypeScript + Node.js；使用 `@deepseek-ai/schemastery` 做配置校验与默认值填充；通过 `@deepseek-ai/dsh-subagent` 的统一 `SubagentProvider`/`SubagentRun` 契约接入 Cordis；进程管理委托给 `@deepseek-ai/dsh-subprocess`；协议层分别对接 ACP SDK (`@agentclientprotocol/sdk`)、Anthropic Claude Agent SDK、Codex app-server JSON-RPC (0.147.0) 与 DeepSeek Harness SDK Client。