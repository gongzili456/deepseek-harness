各包通过各自的 `package.json` 独立发布为 `@deepseek-ai/dsh-subagent-acp|claude-code|codex|dsh-sdk`；运行时需在部署配置中调用对应包的 `apply(ctx, config)` 以注册 provider，并传入 `command`/`args`/`cwd`/`env` 等部署级参数（如 ACP/DSP SDK 需指定子进程可执行文件，Claude Code/Codex 依赖已安装的 CLI）。