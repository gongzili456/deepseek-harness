每个子包均为独立 npm 包（见各自 `package.json`），通过各自的 `apply(ctx, config?)` 入口在宿主中注册：`tool-goal` 调用 `ctx.tools.register`，`command-goal` 调用 `ctx.commands.register`，`goal-round-driver` 通过 `ctx.effect` 安装生命周期监听；构建产物由 `tsdown.config.ts`（core 包）或默认 tsc 输出到 `lib/`。