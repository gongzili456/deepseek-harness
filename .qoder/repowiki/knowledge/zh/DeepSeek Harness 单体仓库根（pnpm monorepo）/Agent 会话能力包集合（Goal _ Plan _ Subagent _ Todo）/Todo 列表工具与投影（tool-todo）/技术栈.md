TypeScript ESM 包，使用 `@deepseek-ai/schemastery` + `zod` 定义工具参数与输出 schema，基于 `@deepseek-ai/dsh-tools` 的 `defineTool` 注册模型工具，通过 `@deepseek-ai/dsh-session` 的事件流追加 `todo/write` 快照，并以 `@deepseek-ai/dsh-invariants` 的 InvariantInstaller 做持久化数据契约校验。