- 每个子包都同时维护 README.md、README.zh.md 与 README.i18n.yaml 三份文档，保持中英文与国际化元数据同步。
- 每个子包 src 目录下均提供 invariant.ts，集中声明运行时不变量并在测试中覆盖。
- 对外暴露的能力以独立 package（如 tool-*、*-driver、*-mode）拆分，通过 index.ts 统一 re-export，便于宿主按需引入。