两个子包均为独立 npm 包，通过 `tsdown.config.ts`（llm-retry）或默认 tsconfig 构建至 `lib/`；测试位于各自 `tests/` 目录，依赖 `@deepseek-ai/dsh-agent-loop-testkit`、`@deepseek-ai/dsh-mock-server`、`@deepseek-ai/dsh-session-persistence-jsonl|sqlite` 等 workspace 包进行端到端回放与持久化验证。