基于 `@modelcontextprotocol/sdk` (v1.12) 的 MCP Client；使用 Zod + `@deepseek-ai/schemastery` 校验配置与输出 schema；通过 `@deepseek-ai/dsh-subprocess` 提供子进程环境变量清洗；peer 依赖 `@deepseek-ai/cordis` 与 `@deepseek-ai/dsh-tools` 集成插件框架与工具注册表。