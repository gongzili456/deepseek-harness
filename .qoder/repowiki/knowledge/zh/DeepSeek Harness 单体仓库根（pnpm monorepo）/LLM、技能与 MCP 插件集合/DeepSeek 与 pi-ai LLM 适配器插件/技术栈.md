TypeScript ESM 包；使用 `@deepseek-ai/schemastery` 做配置 schema 校验；`llm-deepseek` 直接 HTTP + SSE（`eventsource-parser`）调用 DeepSeek `/chat/completions`；`llm-pi-ai` 通过 `@earendil-works/pi-ai` SDK 统一多后端（OpenAI、Anthropic 等）。