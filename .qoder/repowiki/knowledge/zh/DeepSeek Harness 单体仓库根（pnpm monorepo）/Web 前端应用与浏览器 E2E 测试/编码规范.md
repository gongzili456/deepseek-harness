- 每个 e2e 场景文件（`*.e2e.ts`）通过 `launchWebScaffold` 启动同一套 Cordis 组合，并通过 `replayFixture` / `replayOverride` / `toolsMode` 等选项声明式切换行为，而非各自拼装 tree。
- 快照断言采用「稳定化 → 比较或刷新」流程：先用 `captureStableAria` 轮询到 aria snapshot 不变，再通过 `compareOrRefreshGolden` 与 `tests/snapshots/<scenario>/ui.expected.md` 比对，缺失时抛出引导命令。
- fixture 中的运行时标识（sessionId、cwd、rpcId）统一替换为 `{{sessionId}}` / `{{cwd}}` / `{{rpcId}}` 占位符，并通过 `scrubRequestHeaders` 和 `stabilizeFixtureMessageIds` 保证跨机可重现。
- 测试不直接 import `@deepseek-ai/dsh-client-*` 值/类型，而是镜像所需常量（如欢迎通知命名空间）以避免把 Client 项目拖入 Host 编译图。
- Vite 构建将重型第三方包显式列入 `VENDOR_PACKAGES` 集合以拆分 vendor chunk，React 相关代码保持在 index chunk，确保编辑 shell 代码只重哈希 index。