Vite 6 + @vitejs/plugin-react 4 构建 React 18 前端；Playwright 1.49 驱动 Chromium 执行 e2e；Cordis 插件系统（Loader/Include/Group）在进程内组装宿主树；会话回放通过 `@deepseek-ai/dsh-llm-replay` 的 `installLlmReplay` 注入；快照对比基于 aria snapshot 与 `@deepseek-ai/dsh-acp-snapshot`。