TypeScript ESM 包，基于 `@deepseek-ai/dsh-tools` 的 `defineTool` 注册模型工具，使用 `@deepseek-ai/schemastery` 声明 Config 参数 schema，通过 `@deepseek-ai/dsh-workflow` 的 `WorkflowEngine` 执行脚本，`@deepseek-ai/cordis` 的 `Context` 注入 `tools`/`workflowEngine`/`subagents`/`systemPrompt`。