JSONL 后端使用 `koffi ^3.1.0` 调用原生 Zstandard 库进行帧级压缩/解压，并通过 POSIX `link()` + `fsync` 与 Win32 专用 API 实现原子发布；SQLite 后端直接使用 Node.js 内置 `node:sqlite` 的 `DatabaseSync`，默认 WAL journal mode。配置校验统一使用 `@deepseek-ai/schemastery`。