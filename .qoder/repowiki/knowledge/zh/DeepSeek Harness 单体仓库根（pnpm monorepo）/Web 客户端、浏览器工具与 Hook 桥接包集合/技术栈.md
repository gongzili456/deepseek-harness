TypeScript + tsdown 打包；状态管理采用 zustand + immer（defineStore/shallowEqual）；RPC 基于 HTTP bridge 与 WebSocket downlink；测试使用 Vitest（jsdom 环境）并配合 `pnpm run test:gui` 与 `DSH_SNAPSHOT=replay pnpm run test:web` 进行端到端快照回放；hook 协议通过 schemastery 做配置校验。