每个子包都是独立的 npm package（含独立 `package.json`/`tsconfig.json`），通过 `cordis.yml` 在宿主应用中选择一个 executor 实现（如 `bash-local` 或 `bash-sandbox`）并挂载到 `ctx.shell`；沙箱执行器要求同时加载 `ctx.sandbox` 与 `ctx.sandboxPolicy`，否则在工具插件加载阶段即抛出错误。