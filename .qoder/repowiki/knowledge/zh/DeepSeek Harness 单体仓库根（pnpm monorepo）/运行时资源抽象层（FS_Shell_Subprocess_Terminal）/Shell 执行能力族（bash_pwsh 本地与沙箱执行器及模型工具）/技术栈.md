基于 Cordis 服务框架（`@deepseek-ai/cordis`）注册 `ctx.shell`/`ctx.tools`；配置校验使用 `@deepseek-ai/schemastery`；超时/截止期使用 `@deepseek-ai/dsh-timeout`；沙箱后端通过 `@deepseek-ai/dsh-sandbox` 抽象（支持 bwrap/landlock/seatbelt/acl 等运行时检测）。