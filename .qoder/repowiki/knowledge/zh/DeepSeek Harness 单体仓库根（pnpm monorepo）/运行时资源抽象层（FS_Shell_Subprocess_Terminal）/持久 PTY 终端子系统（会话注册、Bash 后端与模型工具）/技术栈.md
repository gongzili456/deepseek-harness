TypeScript ESM 模块，基于 `@deepseek-ai/cordis` 的 Service/Context 插件体系；会话标识通过 `@deepseek-ai/dsh-brand` 的 Branded 类型实现；工具 schema 校验使用 `@deepseek-ai/schemastery`；PTY 底层由 `@deepseek-ai/dsh-subprocess` 的 `spawnTerminal` 提供。