- 每个能力包按「service 接口 / local 实现 / sandbox 实现 / tool 工具」四件套拆分，对外仅暴露 service 层的 ctx 注入点
- 所有入口函数在 src/invariant.ts 中集中做参数校验，测试通过 spec.ts 覆盖正常与异常路径
- 每个包附带 README.md、README.zh.md 与 README.i18n.yaml 三份文档，保持中英文与结构化描述同步
- 沙箱实现通过条件加载（如 win32.ts、helpers.ts）区分平台差异，而非在业务逻辑中分散判断