Typert 生成器通过 `generator/src/tsdown-plugin.ts` 注入 tsdown 构建管线，测试用例位于 `generator/tests/fixtures/remote-model` 与 `type-model` 两个最小工作区；ACP 快照测试需通过 `pnpm run test:snapshot` 触发 acp-snapshot 套件；llm-mock-server 提供 CLI（`bin.ts`）作为 OpenAI 兼容的确定性故障服务器。