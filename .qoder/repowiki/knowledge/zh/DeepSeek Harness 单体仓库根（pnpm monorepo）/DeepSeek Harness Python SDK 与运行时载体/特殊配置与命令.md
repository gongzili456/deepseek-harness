`deepseek-harness-runtime-bin` 是 wheel-only 包，构建时必须设置 `DSH_RUNTIME_PLATFORM_TAG`（或通过本机平台推断）并在 `src/deepseek_harness_runtime/runtime/` 下放置对应 `dsh-jsonrpc-agent-pkg-*` 可执行；生产部署需先运行 `scripts/build-exe-for-python-sdk.ts` 生成二进制，再安装该 wheel。本地开发可通过 `DSH_RUNTIME_MODE=node` 切换到源码 node 闭包模式。